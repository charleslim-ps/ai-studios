{
  "name": "ai-studios-composer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Scheduled composer: pulls Linear + GitHub, applies the health rule, writes app/public/studios.json.",
  "scripts": {
    "compose": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
